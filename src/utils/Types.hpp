#ifndef TYPES_HPP_
#define TYPES_HPP_

#include <iterator>
#include <string>
#include <vector>

typedef std::vector<Line>::iterator LineIter;

typedef std::vector<std::string>::iterator StringIter;

#endif  // TYPES_HPP_
